# ADR 0007: Git 브랜치 및 커밋 거버넌스

**날짜**: 2026-03-19
**상태**: 수락됨 (Accepted)
**결정자**: core-platform
**발견 경위**: 코어 플랫폼은 자동 업그레이드가 반복되지만, 저장소에 브랜치 전략·커밋 규칙·PR 기준이 고정돼 있지 않았다.

## 맥락

Workflow OS 코어는 앞으로 여러 도메인과 여러 저장소를 연결하는 기준 저장소가 된다.
이 저장소에서 브랜치 전략이 흔들리면 다음 문제가 생긴다.

1. 어떤 변경이 어떤 의도로 들어왔는지 추적이 어렵다.
2. memory, ADR, worklog, 코드가 서로 다른 시점으로 어긋난다.
3. 다른 프로젝트와 연결할 때 release evidence를 남기기 어렵다.
4. 자동화 에이전트가 안전하게 작은 단위 커밋을 만들기 어렵다.

## 결정

Git 운영 기준을 아래처럼 고정한다.

### 1. 기본 브랜치

- trunk 브랜치는 `main`이다.
- `main`은 직접 실험하는 공간이 아니라, 검증을 통과한 결과만 합치는 기준선이다.

### 2. 작업 브랜치

작업 브랜치는 짧게 살고, 목적이 이름에 드러나야 한다.

- `feat/core-...`
- `fix/core-...`
- `docs/core-...`
- `chore/core-...`

예:

- `feat/core-release-evidence`
- `docs/core-git-governance`
- `fix/core-contract-validator`

### 3. 커밋 전략

- 커밋은 "파일 묶음"이 아니라 "의미 단위"로 나눈다.
- 한 커밋은 하나의 이유를 설명할 수 있어야 한다.
- 커밋 메시지는 Conventional Commits 스타일을 따른다.

예:

- `feat(core): add release evidence generator`
- `docs(vcs): define branch and pr workflow`
- `fix(quality): align dependency scan status`

### 4. PR 기준

PR에는 반드시 아래가 함께 있어야 한다.

1. 코드 또는 스크립트 변경
2. 관련 memory 갱신
3. 필요한 ADR 또는 참조 문서 갱신
4. 검증 명령과 결과
5. release evidence 또는 그 경로

### 5. 작업 로그 기준

작업 로그는 "무엇을 바꿨다"에서 끝내지 않는다.
아래 흐름으로 남긴다.

1. 왜 이 작업이 필요한가
2. 이전 상태의 문제는 무엇인가
3. 어떤 파일을 바꿨는가
4. 핵심 코드는 어떤 생각으로 설계했는가
5. 검증 결과는 무엇인가
6. 다음 작업은 무엇인가

## 결과

1. 코어 저장소의 변경 이력이 다른 저장소와 연결하기 쉬운 형태가 된다.
2. 자동화 에이전트도 작은 단위 커밋을 안전하게 만들 수 있다.
3. PR과 release evidence를 기준으로 추적 가능한 운영 흐름이 생긴다.

## 대안

- **main 직접 커밋**
  - 거부: 빠르지만 변경 추적과 rollback 근거가 약해진다.
- **큰 작업을 한 번에 squash**
  - 거부: 학습 가치와 회귀 추적성이 약해진다.
- **문서 없는 자유 브랜치**
  - 거부: 다른 프로젝트와 유기적으로 연결하기 어렵다.

## 후속 작업

- 브랜치/PR 정책 문서 추가
- release evidence 생성 스크립트 추가
- 학습형 작업 로그 템플릿 추가
