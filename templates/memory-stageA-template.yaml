# Stage A Memory Template
# 저장 위치: memory/stageA/[module-id].yaml

module_id: "[module-id]"
bounded_context: "[bounded-context-id]"
domain: "[domain-id]"

ubiquitous_language:
  - term: "[용어]"
    definition: "[정의]"

invariants:
  - id: "INV001"
    description: "[불변조건 설명]"
    enforcement: "도메인 서비스에서 강제"

permissions:
  - role: "[role-name]"
    capabilities: ["[capability-id]"]
    screens: ["[screen-id]"]

contracts:
  http: "contract/openapi.yaml"
  events: "contract/events.schema.json"
  ui: "contract/ui-contract.yaml"
  capability: "contract/capability.yaml"

contract_version: "0.1.0"
contract_hash: "[확인 필요: 계약 파일의 해시값 - CI에서 자동 생성]"

completed_at: "YYYY-MM-DD"
gate_result: "PASS | FAIL"
gate_failures: []
notes: ""
