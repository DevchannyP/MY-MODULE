wp_id: "WP-2026-03-19-08"
goal: "Add a truthful GitHub Actions workflow that automates the repository quality gates already implemented locally."
completed: true
files_created:
  - path: ".github/workflows/quality-gates.yml"
    purpose: "Broad GitHub Actions workflow for implemented Stage D quality gates"
  - path: "worklog/2026-03-19_WP-08.yaml"
    purpose: "Structured evidence for governance automation"
files_modified:
  - path: "docs/reference/quality-gates.md"
    change: "Documented automated CI coverage versus still-manual release evidence and deployment smoke"
  - path: "docs/how-to/run-stage-d.md"
    change: "Linked local Stage D execution to the new quality-gates workflow truthfully"
  - path: "memory/current-state.yaml"
    change: "Promoted broad quality-gate CI from manual placeholder to working capability"
  - path: "memory/wp-queue.yaml"
    change: "Marked WP-2026-03-19-08 done and advanced WP-2026-03-19-10"
  - path: "memory/next-actions.yaml"
    change: "Selected WP-2026-03-19-10 as the next packet"
  - path: "memory/checkpoint.yaml"
    change: "Included WP-2026-03-19-08 in completed packets and updated next-session context"
validation:
  lint: "pass"
  tests: "pass"
  requirements: "pass"
  contract: "pass"
risks:
  - "CI now covers in-repo quality gates, but release evidence generation and environment-level smoke remain outside GitHub Actions."
  - "Local reruns of npm run test:e2e-smoke can still need explicit approval in restricted sandboxes because the smoke binds a port."
decisions:
  - "Keep requirements-validation.yml as a narrow fast-path and add a separate broad quality-gates workflow instead of overloading one file."
  - "Automate only commands that already pass locally and leave release-evidence and deployment-environment checks labeled manual."
commands:
  - "npm test"
  - "npm run test:contract"
  - "npm run type-check"
  - "npm run validate:composition"
  - "npm run test:authn-authz"
  - "npm run scan:dependencies"
  - "npm run check:advisory-policy"
  - "npm run generate:sbom"
  - "npm run verify:provenance"
  - "npm run test:rollback"
  - "npm run check:observability"
  - "npm run scan:secrets"
  - "rg -n \"npm run validate:requirements|npm run lint|npm run test:e2e-smoke\" .github/workflows"
  - "npm run validate:requirements"
  - "npm run lint"
